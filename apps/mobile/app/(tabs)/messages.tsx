import { ComingSoonScreen } from "@/components/ui/ComingSoonScreen";

export default function MessagesScreen() {
  return (
    <ComingSoonScreen
      title="Messages — coming next"
      message="Direct messaging already works on the FanHub web app against this same account and backend. The mobile conversation UI hasn't been built yet in this app — it's next on the roadmap, not skipped."
    />
  );
}
