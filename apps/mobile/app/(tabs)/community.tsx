import { ComingSoonScreen } from "@/components/ui/ComingSoonScreen";

export default function CommunityScreen() {
  return (
    <ComingSoonScreen
      title="Community — coming next"
      message="Posts, Fan Rooms, reactions, and comments are fully built on the FanHub web app and share this same Supabase backend. The mobile feed UI for this screen hasn't been built yet in this app — it's explicitly next on the roadmap, not skipped."
    />
  );
}
